apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata:
  name: dns-spoofing-test
  namespace: default
spec:
  action: random
  mode: one
  selector:
    labelSelectors:
      "app": "backend"
  patterns:
    - "example.com"