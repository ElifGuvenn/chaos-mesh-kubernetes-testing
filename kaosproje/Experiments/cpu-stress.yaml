apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-stress-test
  namespace: default
spec:
  mode: one
  selector:
    labelSelectors:
      "app": "backend"
  stressors:
    cpu:
      workers: 1
      load: 100
  duration: "60s"